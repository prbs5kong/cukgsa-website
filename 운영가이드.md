# KGSA 홈페이지 운영 가이드

> 이 문서 하나만 보고 **개발 지식 없이** 홈페이지를 운영할 수 있게 쓴 문서입니다.
> 임원이 바뀌면 이 문서를 인수인계해 주세요.

---

## 1. 먼저 알아야 할 구조

이 사이트는 **정적 사이트**입니다. 서버도 데이터베이스도 없고, 파일 몇 개가 전부입니다.

- 장점: **공짜**이고, 관리할 서버가 없고, 절대 다운되지 않습니다.
- 단점: **회원이 직접 글을 쓸 수 없습니다.** (→ 4장에서 해결 방법 설명)

내용은 **`assets/js/data.js` 파일 한 개**에 모여 있습니다.
행사, 후원업체, 게시글, 임원진 전부 여기 있습니다. 이 파일만 고치면 됩니다.

---

## 2. 도메인 + 실서비스 오픈 (최초 1회)

### 2-0. 오픈 전 점검 — 명령어 하나로

```bash
cd /Users/wooh2237/Documents/homepage
bash launch-check.sh
```

남아 있는 예시 데이터를 전부 찾아서 알려줍니다.
**★ 표시가 0건이 될 때까지 올리지 마세요.**

특히 중요한 것:

> ⚠️ **후원업체와 채용공고 예시에는 실존하는 회사 이름이 들어 있습니다.**
> (Boulder Dental Care, Rocky Mountain Insurance, Google, NIST, 삼성전자 등)
> 동의 없이 "후원업체"로 올리면 그 회사에 피해가 가고, 항의를 받을 수 있습니다.
> **실제 계약한 곳만 남기고 나머지는 반드시 지우세요.**

> ⚠️ **about.html 의 "설립 2005년 / 등록 회원 250+명 / 연간 행사 20+회" 와 연혁은
> 전부 제가 만들어 넣은 예시 숫자입니다.** 실제 값으로 바꾸거나, 모르면 그 줄을 지우세요.
> 학생회 소개에 틀린 숫자가 있으면 신뢰를 잃습니다.

### 2-0-1. 도메인 어디서 사나요

| 등록업체 | .com 1년 | 특징 |
|---|---|---|
| **Cloudflare Registrar** | **약 $10.4** | 원가 그대로. 첫해 할인 없이 **갱신도 같은 가격** — 장기적으로 가장 쌈. 단, 신규 등록만 가능하고 UI가 영어 |
| **Porkbun** | 약 $11 | WHOIS 비공개·SSL 무료 포함, UI가 쉬움 |
| **Namecheap** | 첫해 저렴하나 갱신 비쌈 | 첫해 미끼가격 주의 |
| **GoDaddy** | 첫해 저렴하나 갱신 매우 비쌈 | 권하지 않음 |

**추천: Cloudflare Registrar 또는 Porkbun.** 둘 다 연 $11 안팎이고,
GoDaddy 같은 곳은 첫해 $1이지만 2년차부터 $20+ 로 뜁니다.
학생회는 회장이 매년 바뀌므로 **갱신가가 일정한 곳**이 안전합니다.

> ⚠️ **자동 갱신을 반드시 켜 두세요.** 만료되면 남이 도메인을 채갈 수 있고,
> 되찾으려면 훨씬 비쌉니다. 결제 카드도 졸업하는 임원 개인 카드보다
> 학생회 공용 카드나 오래 남을 사람 것으로 하세요.

### 2-1. 도메인 설정 — `set-domain.sh`

도메인을 사면 **가장 먼저** 터미널에서 이걸 실행하세요.

```bash
cd /Users/wooh2237/Documents/homepage
bash set-domain.sh cukgsa.org        # 본인이 산 도메인으로
```

이게 하는 일:
- 모든 페이지의 `canonical` / `og:url` / `og:image` 주소를 새 도메인으로 교체
- `sitemap.xml`, `robots.txt` 주소 교체
- GitHub Pages용 `CNAME` 파일 생성

**왜 중요하냐면** — 이걸 안 하면 카카오톡·페이스북에 링크를 공유했을 때
미리보기 카드(사진 + 제목)가 안 뜹니다. 도메인을 바꿔도 다시 실행하면 됩니다.

### 2-2. DNS 설정 (도메인 산 곳에서)

도메인 구입처의 DNS 관리 화면에서 아래를 추가합니다.

| 종류 | 이름 | 값 |
|---|---|---|
| A | `@` | `185.199.108.153` |
| A | `@` | `185.199.109.153` |
| A | `@` | `185.199.110.153` |
| A | `@` | `185.199.111.153` |
| CNAME | `www` | `본인아이디.github.io` |

반영에 최대 24시간 걸릴 수 있습니다. 그 뒤 GitHub → Settings → Pages 에서
**Enforce HTTPS** 를 켜면 `https://` 로 접속됩니다. (인증서 무료·자동)

---

## 2-A. 인터넷에 올리기 (최초 1회)

### 추천: GitHub Pages

**왜 추천하냐면 — 올린 다음에도 브라우저에서 바로 수정할 수 있기 때문입니다.**
컴퓨터에 아무것도 설치할 필요가 없고, 임원이 바뀌어도 계정만 넘기면 됩니다.

1. https://github.com 가입 (무료)
2. 우측 상단 **`+` → New repository**
   - Repository name: `kgsa` (아무 이름이나)
   - **Public** 선택 → **Create repository**
3. 다음 화면에서 **`uploading an existing file`** 클릭
4. `homepage` 폴더 **안의 내용물 전부**를 드래그 앤 드롭
   (`index.html`, `pages` 폴더, `assets` 폴더, `README.md`, `운영가이드.md`)
   ※ `homepage` 폴더 자체가 아니라 **그 안의 것들**을 넣어야 합니다.
5. 아래 **Commit changes** 클릭
6. 저장소 상단 **Settings → Pages**
   - Source: **Deploy from a branch**
   - Branch: **main** / 폴더: **/ (root)** → **Save**
7. 1~2분 뒤 `https://본인아이디.github.io/kgsa/` 주소가 생깁니다.

### 더 간단하지만 수정이 불편한 방법: Netlify Drop

https://app.netlify.com/drop 에 폴더를 통째로 드래그하면 끝입니다. 계정도 필요 없습니다.
다만 내용을 고칠 때마다 **폴더 전체를 다시 드래그**해야 하므로, 장기 운영에는 GitHub Pages를 권합니다.

### 도메인 (`cukgsa.org` 같은 주소)

원하시면 도메인을 사서 연결할 수 있습니다. (연 1~2만원 선)
GitHub Pages, Netlify 모두 무료로 연결되고 HTTPS도 자동입니다.

---

## 3. 내용 수정하기 (매번 하는 일)

> ### ⭐ Supabase 를 연결했다면 — 파일을 고칠 필요가 없습니다
>
> 상단 **운영** 메뉴에서 바로 고칩니다. **저장하면 즉시 사이트에 반영**되고,
> GitHub 에 다시 올릴 필요가 없습니다.
>
> | 운영 메뉴 탭 | 고칠 수 있는 것 |
> |---|---|
> | 신고 처리 | 신고된 글·댓글 숨기기 / 처리 완료 |
> | 회원 · 차단 | 스팸 계정 7일 / 30일 / 영구+글삭제 |
> | **행사** | 다가오는 행사 추가·수정·삭제 (홈 오른쪽) |
> | **후원업체** | 후원업체 추가·수정·삭제 |
> | **임원진** | 임원 이름·직책·학과 (소개 페이지) |
> | **연락처** | 대표 이메일, 인스타·페이스북·카카오 주소 |
>
> 게시글은 회원이 직접 씁니다. 운영진은 공지 고정과 삭제를 할 수 있습니다.
>
> **잘 안 되면** `홈페이지주소/pages/setup-check.html` 에 들어가 보세요.
> 어디서 막혔는지 짚어 줍니다.
>
> **아래 3-1 ~ 3-5 는 Supabase 를 연결하지 않았을 때의 방법입니다.**

### 수정 방법 (GitHub Pages 기준)

1. GitHub 저장소에서 `assets` → `js` → `data.js` 클릭
2. 오른쪽 위 **연필 아이콘(✏️ Edit this file)** 클릭
3. 내용 수정
4. 아래로 내려서 **Commit changes** 클릭
5. **1~2분 뒤** 홈페이지에 반영됩니다. (바로 안 바뀌면 새로고침 `Cmd+Shift+R`)

---

### 3-1. 다가오는 행사 추가/삭제

`data.js`에서 `const KGSA_EVENTS = [` 를 찾으세요. 이렇게 생겼습니다.

```js
const KGSA_EVENTS = [
  { date: "2026-08-29", title: "가을학기 환영 BBQ", desc: "신입생 환영 + 재학생 친목. 음식 제공, 가족 동반 환영!", where: "Chautauqua Park, Boulder", time: "오후 5:00" },
  { date: "2026-09-12", title: "신입생 오리엔테이션 & 정착 세미나", desc: "SSN 신청, 은행 계좌, 운전면허, 보험까지 한 번에 정리", where: "UMC 235", time: "오후 6:00" },
];
```

**추가**하려면 `];` 바로 위 줄에 아래를 복사해 붙여넣고 내용만 바꾸세요.

```js
  { date: "2026-11-15", title: "김장 모임", desc: "재료비만 받고 다 같이 김장합니다", where: "UMC 235", time: "오후 2:00" },
```

| 항목 | 뜻 | 주의 |
|---|---|---|
| `date` | 행사 날짜 | 반드시 `"2026-11-15"` 형식. 홈의 `NOV 15` 칩이 여기서 자동 생성됩니다 |
| `title` | 행사 이름 | |
| `desc` | 한 줄 설명 | 너무 길면 사이드바에서 줄바꿈됩니다. 40자 이내 권장 |
| `where` | 장소 | |
| `time` | 시간 | `"오후 5:00"` 처럼 자유롭게 |

**삭제**는 그 줄을 통째로 지우면 됩니다. 끝난 행사는 지워 주세요.
표시 순서 = 적어 놓은 순서입니다. (날짜순 자동 정렬 아님)

---

### 3-2. 후원업체 추가/삭제

`const KGSA_SPONSORS = [` 를 찾으세요.

```js
  { name: "한국식품 마켓", cat: "한인 마트 · 식료품", initials: "HM", url: "#" },
```

추가할 때:

```js
  { name: "볼더 헤어샵", cat: "미용실 · 한국어 가능", initials: "BH", url: "https://example.com" },
```

| 항목 | 뜻 |
|---|---|
| `name` | 업체 이름 (카드에 굵게 표시) |
| `cat` | 업종 한 줄 (작게 표시) |
| `initials` | **로고 자리에 들어갈 영문 2글자.** 예: Boulder Dental → `"BD"` |
| `url` | 업체 홈페이지. 없으면 `"#"` |

> **로고 이미지를 넣고 싶다면?** 지금은 골드 박스에 영문 2글자가 들어가는 방식입니다.
> 실제 로고 이미지로 바꾸려면 코드 수정이 조금 필요합니다 — 필요해지면 말씀해 주세요.

---

### 3-3. 게시글 추가 (공지·취업·벼룩시장 등)

`const KGSA_POSTS = [` 를 찾아 아래 한 줄을 추가하세요.

```js
  { id: 100, cat: "notice", title: "2026 가을 총회 안내", date: "2026-09-01", author: "운영진", badge: "new", link: "#" },
```

| 항목 | 넣을 수 있는 값 |
|---|---|
| `id` | 겹치지 않는 아무 숫자 |
| `cat` | `"notice"`(공지사항) `"career"`(취업·인턴) `"market"`(벼룩시장) `"housing"`(하우징) `"community"`(자유게시판) |
| `badge` | `"pin"`(맨 위 고정) `"new"` `"hot"` `null`(배지 없음) |
| `link` | 클릭 시 이동할 주소. 구글 문서·공고 링크 등. 없으면 `"#"` |

> **팁:** 글 내용이 긴 공지는 **구글 문서(Google Docs)로 쓰고 "링크가 있는 모든 사용자에게 공개"로 바꾼 뒤,
> 그 주소를 `link` 에 넣는 것**이 가장 편합니다. 홈페이지에는 제목만 뜨고 클릭하면 문서로 갑니다.

---

### 3-4. 임원진 교체

`const KGSA_OFFICERS = [` 에서 이름·직책·학과를 바꾸면 됩니다.
동그라미 안 글자는 이름 첫 글자가 자동으로 들어갑니다.

---

### 3-5. 그 밖의 것들

| 바꾸고 싶은 것 | 파일 |
|---|---|
| 도메인 | `bash set-domain.sh 새도메인.org` |
| 카톡·페북 공유 미리보기 이미지 | `assets/img/og-cover.jpg` (1200×630) |
| 상단 배너 사진 | `assets/img/cu-campus.jpg` 를 같은 이름의 다른 사진으로 교체 |
| 메뉴 구성 | `assets/js/main.js` 맨 위 `NAV` 배열 |
| 이메일 주소 | `assets/js/main.js` 에서 `kgsa@colorado.edu` 를 전부 바꾸기 |
| 인스타/카톡 링크 | `assets/js/main.js` 의 `renderFooter` 안 `href="#"` |
| 생활정보 내용 | `pages/info.html` (이건 HTML 직접 수정) |
| 색상 | `assets/css/style.css` 맨 위 `:root` 블록 |

---

## 4. 회원 게시판 (구글 로그인)

**코드는 전부 준비돼 있습니다.** Supabase 계정만 만들어 연결하면 켜집니다.

→ 설치 절차: **[supabase/설치방법.md](supabase/설치방법.md)** (30~40분, 무료)

연결하면 되는 것:
- 구글 계정으로 로그인 → 회원 누구나 글쓰기·수정·삭제
- 사진 첨부 (글당 8장, 장당 5MB)
- 댓글
- 신고 버튼 → 운영진 화면에 쌓임
- **스팸 계정 차단** — 상단 **운영** 메뉴에서 7일 / 30일 / 영구+글삭제.
  **웹에서 바로 되고 사이트를 다시 올릴 필요가 없습니다.**

### 연결하기 전에는?
게시판이 `data.js` 의 예시 데이터로 그대로 돌아갑니다. 사이트는 정상 작동하고,
글쓰기 페이지에 들어가면 "아직 설정되지 않았습니다" 안내가 뜹니다.

### 차단이 왜 재배포 없이 되나요?
차단 여부가 **코드가 아니라 데이터베이스**에 있기 때문입니다.
운영 화면에서 차단하면 그 즉시 DB의 권한 규칙(RLS)이 그 계정의 글쓰기·댓글·사진 업로드를
전부 거부합니다. 브라우저 쪽을 아무리 조작해도 우회되지 않습니다.

---

## 5. 고장났을 때

### 홈페이지에 빨간 경고가 떴어요

> "콘텐츠를 불러오지 못했습니다"

`data.js` 를 수정하다가 **쉼표( , )나 따옴표( " )를 빠뜨린 것**입니다. 가장 흔한 실수입니다.

1. 브라우저에서 **F12** → **Console** 탭
2. 빨간 글씨에 `data.js:79` 처럼 **줄 번호**가 나옵니다
3. 그 줄과 **바로 윗줄**을 확인하세요. 십중팔구 줄 끝 쉼표가 빠졌습니다

체크리스트:
- 각 줄 끝에 쉼표 `,` 가 있는가 (마지막 줄은 없어도 됨)
- 따옴표가 짝을 이루는가 — `"제목"` ⭕ / `"제목` ❌
- 한글 따옴표(`“ ”`)가 아닌 **영문 따옴표(`" "`)** 인가 ← 복붙할 때 자주 발생
- 중괄호 `{ }` 와 대괄호 `[ ]` 짝이 맞는가

### 수정했는데 안 바뀌어요
- GitHub Pages는 반영에 1~2분 걸립니다
- 브라우저 강력 새로고침: **Mac `Cmd+Shift+R`** / **Windows `Ctrl+F5`**

### 완전히 망가졌어요
GitHub 저장소 → 파일 → **History** 에서 이전 버전으로 되돌릴 수 있습니다.
그래서 GitHub Pages를 추천하는 것입니다. **수정 기록이 전부 남습니다.**

---

## 6. 수정 전 체크리스트

- [ ] 고칠 건 대부분 `assets/js/data.js` 하나입니다
- [ ] 기존 줄을 **복사해서** 내용만 바꾸세요. 처음부터 타이핑하지 마세요
- [ ] 저장 후 **홈페이지를 직접 열어 확인**하세요 (빨간 경고가 없는지)
- [ ] 지난 행사·만료된 공고는 주기적으로 지워 주세요


---

## 7. 실서비스용으로 추가된 파일

| 파일 | 역할 | 손댈 일 |
|---|---|---|
| `set-domain.sh` | 도메인 일괄 설정 | 도메인 바뀔 때만 실행 |
| `CNAME` | GitHub Pages 커스텀 도메인 | 스크립트가 자동 생성 |
| `sitemap.xml` | 검색엔진에 페이지 목록 제공 | 페이지를 새로 만들면 한 줄 추가 |
| `robots.txt` | 검색엔진 수집 허용 | 보통 안 건드림 |
| `404.html` | 없는 주소로 들어왔을 때 | 보통 안 건드림 |
| `assets/img/og-cover.jpg` | 카톡·페북 공유 미리보기 카드 | 사진 바꾸고 싶을 때 |

### 검색에 뜨게 하려면
오픈 후 [Google Search Console](https://search.google.com/search-console) 에
도메인을 등록하고 `sitemap.xml` 주소를 제출하세요. 며칠 뒤부터 검색에 잡힙니다.
